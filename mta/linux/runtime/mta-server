#!/bin/sh
set -eu

SERVER_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
export LD_LIBRARY_PATH="$SERVER_DIR:$SERVER_DIR/mods/deathmatch${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"

exec "$SERVER_DIR/mta-server.bin" --config mtaserver.conf "$@"
