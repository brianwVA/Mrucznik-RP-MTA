-- Vice City was intentionally disabled for this server.
-- Keep an empty shared registry so mrp_models can start without the heavy
-- vc4samp catalog and without requesting those assets from clients.
MRP_OBJECT_MODELS = MRP_OBJECT_MODELS or {}
